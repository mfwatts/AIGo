package game

import (
  "testing"
  //"fmt"
)

func Test_isLegal(t *testing.T) {
  if false {
    t.Errorf("isLegal failure %v",true)
  }
}

func Test_playMove(t *testing.T) {
  if false {
    t.Errorf("playMove failure %v",true)
  }
}
